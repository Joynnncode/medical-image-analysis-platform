# Versions that overlap with ai-service/requirements.txt are pinned to match
# it, so the stub behaves like the service it stands in for.
pytest==8.3.4
requests==2.32.3
nibabel==5.3.2
numpy==1.26.4
psycopg[binary]==3.2.3
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.19
